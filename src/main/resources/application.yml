spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demoapi
    username: root
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: DemoApi

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.tuusuario: DEBUG